const {
    REACT_BASE_URL_SERVER,
    REACT_API_VERSION,
} = process.env

const CONSTANTS_COLLECTION = Object.freeze({
    BASE_URL_SERVER: 'http://localhost:5000/api',
    API_VERSION: 'v1',
})

export default CONSTANTS_COLLECTION
